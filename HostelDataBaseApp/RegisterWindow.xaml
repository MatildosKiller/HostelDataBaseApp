<Window x:Class="HostelDataBaseApp.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HostelDataBaseApp"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="270" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Width="200"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Width="200"/>

        <TextBlock Text="Подтверждение пароля:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,10"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Width="200"/>

        <TextBlock Text="ФИО:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,10"/>
        <TextBox x:Name="FullNameTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,20" Width="200"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button x:Name="RegisterButton" Content="Зарегистрироваться" Click="RegisterButton_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Margin="10"/>
        </StackPanel>

        <TextBlock  x:Name="ErrorTextBlock" Grid.Row="5" Grid.ColumnSpan="2" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Window>
