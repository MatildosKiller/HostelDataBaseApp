<Window x:Class="HostelDataBaseApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="200" Width="300"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Width="150"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Width="150"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoginButton" Content="Войти" Click="LoginButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button x:Name="RegisterButton" Content="Регистрация" Click="RegisterButton_Click" Width="80"/>
        </StackPanel>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="3" Grid.ColumnSpan="2" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Window>
